# Core runtime dependencies for Sprtz.
# Install with:
#   python -m pip install -r requirements.txt
# Optional feature groups remain available through pyproject extras, for example:
#   python -m pip install -e '.[netcdf,viz,mpi]'

numpy>=1.23
pyproj>=3.4
