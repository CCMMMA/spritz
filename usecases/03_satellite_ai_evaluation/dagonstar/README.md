# 03 Satellite Ai Evaluation Dagonstar Workflow

This directory contains a Dagonstar-oriented workflow sketch for the use case. Dagonstar is maintained at https://github.com/dagonstar/dagonstar and should be installed/configured outside Sprtz.

`workflow.py` invokes the canonical scripts-only shell pipeline, matching the
CWL workflow and `../pipeline/README.md`. Bind environment overrides in the
workflow-engine layer before operational use.
