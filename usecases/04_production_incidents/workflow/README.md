# 04 Production Incidents WCL Workflow

`usecase.wcl` is a formal, engine-neutral WCL description of the demo steps for this use case. It references the runnable scripts under `../demo/` and is intended to be adapted by an operational workflow engine that can bind inputs, outputs, retries, and resource constraints.

Run the demo commands from the repository root and consult `../pipeline/README.md` for the explicit step order.
